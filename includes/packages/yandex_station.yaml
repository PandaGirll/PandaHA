yandex_station:
  yandex_station:
    recognition_lang: ru-RU
    devices:
      # ff98f029c9ffcb7c6fd30969:  # device_id вашей колонки
        # host: 192.168.0.105
        # name: Яндекс Мини  # имя можно задать и тут
      xk00000000000002957000003a087e6e:  # device_id вашей колонки
        host: 192.168.0.200
        name: Яндекс Станция Макс  # имя можно задать и тут
      m100wv100324ck:
        host: 192.168.0.176
        name: Яндекс Мини 2 кухня # имя можно задать и тут
      # m1047w200mafhk:
        # host: 192.168.0.194
        # name: Яндекс Мини-2 спальня  # имя можно задать и тут
      mk0000000000000011440000dd4905f9:
        host: 192.168.0.176
        name: Яндекс Мини 2 СЧ # имя можно задать и тут
      u00rg1000kknak:
        host: 192.168.0.124
        name: Яндекс Станция 2 # имя можно задать и тут
      d00k88900rra5w:
        host: 192.168.0.206
        name: Модуль # имя можно задать и тут


    include:
    - Кондиционер  # имя вашего кондиционера
    - Магнитола
    - Старый ящик
    - Кондей 2
    - Вентилятор

  sensor:
  ##### Max
  - platform: template
    sensors:
      alice_max_state:
        value_template: "{{ state_attr('media_player.yandex_station_xk00000000000002957000003a087e6e', 'alice_state') }}"

  - platform: template
    sensors:
      alice_max_media:
        value_template: "{{ state_attr('media_player.yandex_station_xk00000000000002957000003a087e6e', 'media_title') }}"
  ##### Mini
  - platform: template
    sensors:
      alice_mini_state:
        value_template: "{{ state_attr('media_player.yandex_station_ff98f029c9ffcb7c6fd30969', 'alice_state') }}"

  - platform: template
    sensors:
      alice_mini_media:
        value_template: "{{ state_attr('media_player.yandex_station_ff98f029c9ffcb7c6fd30969', 'media_title') }}"
  # ##### Mini2
  # - platform: template
    # sensors:
      # alice_mini2_state:
        # value_template: "{{ state_attr('media_player.yandex_station_m1047w200mafhk', 'alice_state') }}"

  # - platform: template
    # sensors:
      # alice_mini2_media:
        # value_template: "{{ state_attr('media_player.yandex_station_m1047w200mafhk', 'media_title') }}"

  ##### Mini2 кухня
  - platform: template
    sensors:
      alice_mini2kitchen_state:
        value_template: "{{ state_attr('media_player.yandex_station_m100wv100324ck', 'alice_state') }}"

  - platform: template
    sensors:
      alice_mini2kitchen_media:
        value_template: "{{ state_attr('media_player.yandex_station_m100wv100324ck', 'media_title') }}"

  ##### Mini2 с часами
  - platform: template
    sensors:
      alice_mini2clock_state:
        value_template: "{{ state_attr('media_player.yandex_station_mk0000000000000011440000dd4905f9', 'alice_state') }}"

  - platform: template
    sensors:
      alice_mini2clock_media:
        value_template: "{{ state_attr('media_player.yandex_station_mk0000000000000011440000dd4905f9', 'media_title') }}"

  ##### Станция 2
  - platform: template
    sensors:
      alice_station2_state:
        value_template: "{{ state_attr('media_player.yandex_station_u00rg1000kknak', 'alice_state') }}"

  - platform: template
    sensors:
      alice_station2_media:
        value_template: "{{ state_attr('media_player.yandex_station_u00rg1000kknak', 'media_title') }}"
